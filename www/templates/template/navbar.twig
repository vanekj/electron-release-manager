<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container">
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbar">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item">
					<a class="btn btn-success er-upload" href="#">
						Upload new version <i class="fa fa-upload"></i>
					</a>
				</li>
			</ul>

			<ul class="navbar-nav ml-auto">
				<li class="nav-item{{ routeName == 'dashboard.manage-versions' ? ' active': '' }}">
					<a class="nav-link" href="{{ path_for('dashboard.manage-versions') }}">Manage versions</a>
				</li>

				<li class="nav-item mx-2{{ routeName == 'dashboard.upload-tokens' ? ' active': '' }}">
					<a class="nav-link" href="{{ path_for('dashboard.upload-tokens') }}">Upload tokens</a>
				</li>

				<li class="nav-item">
					<a class="nav-link text-danger" href="{{ path_for('auth.logout') }}"><i class="fa fa-sign-out-alt"></i></a>
				</li>
			</ul>
		</div>
	</div>
</nav>
